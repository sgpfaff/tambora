* new mechanism to turn self-gravity on and off -- and that things run like they're supposed to when the toggle has been used.
* `NullSelfGravity`
* warn when provides method but self-gravity off
* methods properly combine the different external forces